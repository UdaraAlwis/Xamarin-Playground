<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AdvPrismTabNavigation.Views.DetailPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mvvm="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    Title="{Binding Title}"
    mvvm:ViewModelLocator.AutowireViewModel="True">
    <ContentPage.Content>

        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand">
            <Label Text="Welcome to Xamarin Forms and Prism!" />
            <Button Command="{Binding GoBackToTabChild1PageCommand}" Text="Go back to Tab Child 1 page" />

            <Button Command="{Binding GoBackToTabChild2PageCommand}" Text="Go back to Tab Child 2 page" />

            <Button Command="{Binding GoBackToTabChild3PageCommand}" Text="Go back to Tab Child 3 page" />
        </StackLayout>

    </ContentPage.Content>
</ContentPage>